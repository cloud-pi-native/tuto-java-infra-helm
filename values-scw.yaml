image:
  repository: harbor.external.cpin.numerique-interieur.com/test-2/java-demo
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "tuto"

ingress:
  host: test-18.formation-app.cpin.numerique-interieur.com

# Contrainte de sécurité Openshift
postgres:
  auth:
    existingSecret: pg-secret
    secretKeys:
      passwordKey: "PG_PASSWORD"
      userPasswordKey: "PG_PASSWORD"
      adminPasswordKey: "PG_ROOT_PASSWORD"
    username: "userdemo"
    database: "userdemo"

